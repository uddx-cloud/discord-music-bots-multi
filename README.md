# مشروع 7 بوتات ميوزك ديسكورد (نظام البوت القائد + البقاء المتصل) 🚀

هذا المشروع يتيح لك تشغيل 7 بوتات ديسكورد من كود واحد، مع ميزات متقدمة للتحكم والاستقرار.

## المميزات ✨
- **البوت القائد (Master Bot)**: البوت الأول في القائمة هو المسؤول عن التحكم بالباقي.
- **البقاء المتصل (Auto-Reconnect)**: إذا خرج البوت من الروم (بسبب طرد أو مشكلة اتصال)، سيعود تلقائياً بعد 5 ثوانٍ.
- **التحكم المركزي**: استدعاء أو طرد أي بوت عبر أوامر يرسلها البوت القائد.
- **اتصال تلقائي**: البوتات تتصل بالرومات المحددة لها فور التشغيل.

## ميزة البقاء المتصل 🛡️
تمت إضافة نظام مراقبة ذكي:
1. يقوم البوت بحفظ "الروم الحالي" الذي يجب أن يتواجد فيه.
2. في حال انقطاع الاتصال أو الخروج، يحاول البوت إعادة الاتصال تلقائياً.
3. عند استخدام أمر `!dismiss` أو `!dismissall` من البوت القائد، يتم إيقاف إعادة الاتصال التلقائي لهذا البوت.

## أوامر البوت القائد (Master Bot Commands) 👑
- `!summon [رقم البوت]`: استدعاء بوت معين وتثبيته في رومك.
- `!dismiss [رقم البوت]`: إخراج بوت معين وإيقاف إعادة الاتصال التلقائي له.
- `!summonall`: استدعاء **جميع** البوتات وتثبيتها في رومك.
- `!dismissall`: إخراج **جميع** البوتات وإيقاف ميزة البقاء المتصل للكل.
- `!status`: التأكد من حالة البوت والروم المتصل به.

## طريقة التركيب 📥
1. قم بتحميل المشروع.
2. نفذ `npm install`.
3. عدل `config.json` وضع التوكنات وايدي الرومات.
4. شغل المشروع: `node index.js`.

### التشغيل باستخدام Docker (الموصى به) 🐳
1. تأكد من تثبيت Docker و Docker Compose.
2. قم بتشغيل الأمر التالي لبناء وتشغيل الحاوية:
   ```bash
   docker-compose up -d
   ```
3. لمتابعة السجلات (Logs):
   ```bash
   docker-compose logs -f
   ```

---
تم التحديث بواسطة Manus 🤖
